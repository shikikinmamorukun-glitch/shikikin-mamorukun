# お問い合わせフォーム × Google スプレッドシート連携 セットアップ手順

このサイトの問い合わせを「自分のGoogleスプレッドシート」に自動記録し、同時にGmailへ通知を飛ばすための手順です。所要時間：約10分。**すべて無料**で運用できます。

## 完成イメージ

- お客様がフォームを送信 → 数秒で自分のスプレッドシートに新しい行が追記される
- 同時に、自分のGmailに「新規お問い合わせが届きました」というメールが届く
- 過去のお問い合わせは、シートでいつでも一覧確認できる

---

## STEP 1：Googleスプレッドシートを作成する

1. [https://sheets.google.com](https://sheets.google.com) にアクセス
2. 「**＋ 空白**」をクリックして新しいスプレッドシートを作成
3. シート名を分かりやすく変更（例：「敷金まもるくん_お問い合わせ管理」）

※ シートのヘッダー行はGASが自動で作るので、空のままでOKです。

---

## STEP 2：Apps Script エディタを開く

1. 作成したスプレッドシート上部メニューの「**拡張機能**」→「**Apps Script**」をクリック
2. 新しいタブで Apps Script エディタが開きます
3. 左側のファイル一覧で「**コード.gs**」を選択

---

## STEP 3：GASコードを貼り付ける

1. エディタに最初から書かれている `function myFunction() { ... }` をすべて削除
2. 同梱の `gas-code.gs` の中身をコピーして貼り付け
3. 上部の「**プロジェクト名**」をクリックして、わかりやすい名前に変更（例：「お問い合わせ受信」）
4. **NOTIFY_EMAIL** の値が自分のメールアドレス（adidasyskn.09@gmail.com）になっていることを確認
5. フロッピーディスクのアイコン（または `Ctrl+S` / `Cmd+S`）で保存

---

## STEP 4：ウェブアプリとしてデプロイする

1. 画面右上の「**デプロイ**」→「**新しいデプロイ**」をクリック
2. 左上の歯車アイコン → 「**ウェブアプリ**」を選択
3. 以下のように設定：

   | 項目 | 値 |
   |------|------|
   | 説明 | `お問い合わせ受信 v1`（任意） |
   | 次のユーザーとして実行 | **自分** |
   | アクセスできるユーザー | **全員**（※「全員（匿名含む）」となっている場合もそれを選択） |

4. 「**デプロイ**」をクリック
5. 初回のみ「承認が必要です」と表示されます：
   - 「アクセスを承認」をクリック
   - 自分のGoogleアカウントを選択
   - 「このアプリは Google で確認されていません」と警告が出たら、左下の「**詳細**」→「**プロジェクト名（安全ではないページ）に移動**」をクリック
   - 「**許可**」をクリック
6. 完了画面に表示される「**ウェブアプリ**」の URL（`https://script.google.com/macros/s/.../exec` の形式）を**コピー**します

---

## STEP 5：HTMLにURLを貼り付ける

1. `index.html` をエディタで開く
2. `Ctrl+F` / `Cmd+F` で `GAS_ENDPOINT` を検索
3. 以下の行を見つける：

   ```js
   const GAS_ENDPOINT = "https://script.google.com/macros/s/REPLACE_WITH_YOUR_DEPLOY_ID/exec";
   ```

4. ダブルクォート内のURLを、**STEP 4でコピーしたURLに置き換え**て保存

---

## STEP 6：テスト送信する

1. `index.html` をブラウザで開く（公開済みならサイトURLでもOK）
2. お問い合わせフォームにテスト入力して送信
3. 数秒後：
   - スプレッドシートに新しい行が追加されていることを確認
   - 自分のGmailに通知メールが届いていることを確認

---

## トラブルシューティング

| 症状 | 対処法 |
|------|--------|
| 送信に失敗しました と表示される | デプロイのアクセス権限が「全員」になっているか確認 |
| シートに記録されない | GASの「実行ログ」で `doPost` のエラーを確認 |
| 通知メールが届かない | 迷惑メールフォルダを確認 / `NOTIFY_EMAIL` の綴りを確認 |
| 「Google で確認されていません」警告 | 自分が作ったGASなので「詳細」→「移動」で承認すればOK |

---

## コードを更新した場合

GAS のコードを修正したら、必ず「**デプロイ**」→「**デプロイを管理**」→ 鉛筆アイコン →「**バージョン：新しいバージョン**」を選んで再デプロイしてください。**同じURLのまま反映されます**（新規デプロイすると URLが変わり、HTMLの貼り直しが必要になります）。

---

## セキュリティ・運用上のメモ

- GASのURLが漏れても、できるのは「フォーム送信」だけです（読み取り不可）
- 大量送信のスパム対策として、後から `reCAPTCHA` や `Honeypot` 方式を追加することも可能です
- Gmailの1日あたり送信上限は通常100通／日（GASからの自動メール）です。問い合わせが極端に多い場合のみご注意ください
